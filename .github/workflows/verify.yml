name: Verify TripNow

# TEMPORARILY PAUSED
# GitHub Actions quota is exhausted. Keep this workflow manual-only
# until 2026-09-01. Do not add push/pull_request triggers before then.
on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm install
      - run: npm run typecheck
      - run: npx expo-doctor
